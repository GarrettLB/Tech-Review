<div class="text-center">
  <h2>{{post.title}}</h2>
  <p>{{post.content}}</p>
  <p>Posted by 
    {{post.user.username}} 
    on {{format_date post.time}}</p>
  <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p>
</div>